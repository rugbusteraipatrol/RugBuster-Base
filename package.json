{
  "name": "rugbuster-base",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "deploy:scanner:base": "hardhat run scripts/deploy_scanner_base.js --network base",
    "deploy:activity:base": "hardhat run scripts/deploy_activity_logger.js --network base",
    "verify:scanner:base": "hardhat verify --network base",
    "abi:scanner": "hardhat run scripts/generate_scanner_abi.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.0.2",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.19"
  }
}
